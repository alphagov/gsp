---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "${release_name}-web-configuration"
  namespace: "${namespace}"
data:
  config.yaml: |
    roles:
    - name: viewer
      github:
        teams: ${teams}
