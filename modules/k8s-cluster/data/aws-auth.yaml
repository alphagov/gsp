apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapRoles: |
${bootstrapper_role_mappings}
${iam_admin_role_mappings}
${iam_sre_role_mappings}
${iam_dev_role_mappings}
