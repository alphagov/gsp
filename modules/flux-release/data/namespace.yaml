---
apiVersion: v1
kind: Namespace
metadata:
  name: ${namespace}
  labels:
    namespace: ${namespace}
${extra_namespace_labels}
  annotations:
    iam.amazonaws.com/permitted: "${permitted_roles_regex}"
